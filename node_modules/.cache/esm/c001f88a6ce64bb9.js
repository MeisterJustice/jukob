let mysql,dbconfig,User;_6ce‍.x([["isLoggedIn",()=>isLoggedIn],["checkIfUserExists",()=>checkIfUserExists],["isValidPassword",()=>isValidPassword]]);_6ce‍.w("mysql",[["default",["mysql"],function(v){mysql=v}]]);_6ce‍.w("../config/database",[["default",["dbconfig"],function(v){dbconfig=v}]]);_6ce‍.w("../models/index",[["default",["User"],function(v){User=v}]]);

var connection = mysql.createConnection(dbconfig.connection);



       const isLoggedIn = (req, res, next) => {
    if (req.isAuthenticated()) {
        return next();
    }
    req.flash("error", "You need to be logged in to do that");
    req.session.redirectTo = req.originalUrl;
    res.redirect("/login");
}

       const checkIfUserExists = async (req, res, next) => {
    let emailExists = await User.findOne({ 'email': req.body.email });
    let userNameExists = await User.findOne({ 'username': req.body.username });
    if (emailExists) {
        req.flash('error', `A user with the given email, ${emailExists} is already registered`);
        return res.redirect('/register');
    }
    if (userNameExists) {
        req.flash('error', `A user with the given username, ${userNameExists} is already registered`);
        return res.redirect('/register');
    }
    next();
}

       const isValidPassword = async (req, res, next) => {
    const { user } = await User.authenticate()(req.user.username, req.body.currentPassword)
    if (user) {
        res.locals.user = user;
        next();
    } else {
        req.flash('error', 'current password is incorrect!');
        return res.redirect('back');
    }
}